Delegate to the `pr-writer` subagent: draft a pull request title and
description for the current branch against its base branch (detect the
base; default to main). Show me the draft ready to paste. If the
Testing section claims anything, it must match what was actually run in
this session. $ARGUMENTS
