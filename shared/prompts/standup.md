Delegate to the `git-historian` subagent: summarize what changed in this
repository since $ARGUMENTS (default: yesterday) — commits grouped by theme,
authors, files with the most churn, anything that looks unfinished. Relay
it as a short standup-style update (under 15 lines).
