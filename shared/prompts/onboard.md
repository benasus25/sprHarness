Follow the codebase-onboarding skill: build a one-screen map of this
repository using the `scout`, `dependency-scout`, `bulk-reader`, and
`git-historian` subagents (do not explore the tree or read large files in
this context). Present the map in at most 15 bullets, then ask me what to
work on. $ARGUMENTS
