Explain the following without pulling large files into this context: use
the `scout` subagent to locate the relevant files, then ask the
`bulk-reader` subagent one specific question per file. Synthesize their
answers into a concise explanation with `file:line` references.

Topic: $ARGUMENTS
