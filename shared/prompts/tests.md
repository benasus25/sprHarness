Delegate to the `test-writer` subagent: write tests for the following
target using this project's existing test framework and conventions, run
them, and report a summary. Relay its summary to me, including any code
defects it uncovered.

Target: $ARGUMENTS
