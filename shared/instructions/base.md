# Team baseline for AI coding agents

<!-- This block is installed user-level: into ~/.claude/CLAUDE.md (Claude
     Code) and ~/.codex/AGENTS.md (Codex) as a managed marker block. Cursor
     has no user-level instructions file; use `harness link` per project.
     Keep it short — every session pays for these tokens. -->

- Prefer small, reviewable changes; explain non-obvious decisions in the PR description.
- Never commit secrets, tokens, or `.env` files; flag any you encounter.
- Match the existing style of the file you are editing before applying general style rules.
- When tests exist, run the narrowest relevant test before declaring a change done.
